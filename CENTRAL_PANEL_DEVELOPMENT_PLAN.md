# 多节点 X-Panel 轻量中央管理面板开发进度表

## 1. 排期说明

本计划按一个小型开发团队制定：

- 1 名后端/Agent 开发；
- 1 名前端开发；
- 1 名兼职测试和部署人员；
- 产品确认由项目负责人完成。

基线周期为 10 周。若只有 1 名全栈开发，预计为 14 至 16 周；如果已有通用后台模板和部署脚本，可压缩到 8 周。日期以项目启动日为第 1 天，周末不计入工作日。

前端基线为 Soybean Admin。开发时建议将前端基座、中央后端、节点 Agent 和测试运维分成不同窗口并行推进；各窗口通过 API 契约、类型文件和测试夹具协作，不直接依赖对方未提交的临时实现。

## 1.1 当前执行检查点（2026-09-03）

第一部分已开始落地，当前完成的是前端基座和中央 API 契约，不代表后端和 Agent 已完成：

- 已固定 Soybean Admin 上游 commit `12854ad5f871477503666e758dc7d7edc4f0f277`，前端目录为 `frontend/`；
- 已移除模板首页、iframe 页面、演示登录方式和示例路由，默认首页为 `/dashboard`；
- 已建立 `/dashboard`、`/users`、`/nodes`、`/routes`、`/exit-ips`、`/finance`、`/events` 业务路由；
- 已建立中央 API 类型和 client：Dashboard、用户、节点、线路、出口 IP、财务、事件；
- 用户和节点页面已具备关键词/状态筛选、分页、真实 API 请求、加载/空数据/错误状态；
- 已移除模板 Apifox 请求头，浏览器存储前缀改为 `XPC_`，关闭尚未接入发布检查接口的自动更新提示；
- 已执行 `pnpm typecheck` 和 `pnpm build`，均通过；
- 前端默认请求 `http://localhost:8090/api`；服务未启动时页面显示接口不可用状态，不使用 Mock 数字。

本轮后端骨架已完成：

- `backend/` 已初始化 Go 1.27 工程，使用 SQLite WAL 和幂等迁移；
- 已实现 `/health/live`、`/health/ready`、管理员登录/刷新/退出、当前管理员信息；
- 已实现 Dashboard、用户、节点、线路、出口 IP、财务和同步事件只读 API；
- 已固定统一响应 `{ code, msg, data }`、Bearer Session、过期码 `9999` 和未授权码 `8888`；
- 已加入 OpenAPI 草稿、启动说明和认证/列表集成测试；
- Agent 基础工程已建立：YAML/环境变量配置、X-Panel Session Cookie 登录、401 单次重登录、失败退避、中央 Bearer 客户端、请求 ID/时间戳和 systemd 部署文件；
- Agent 采集侧已建立：Inbound/Client 字段兼容映射、`up/down/all_time` 累计流量读取、节点状态解析、标准化 `config_hash`、心跳和完整同步 payload，以及按 `sync_interval` 运行的可取消任务循环；
- 中央侧 Agent 注册、Bearer 节点认证、心跳接收、完整同步事务、Inbound/Client/流量快照落库和 `sync_id` 幂等已实现；
- 已实现线路机 Inbound 到中央业务用户的一对一自动映射；同步仅刷新 X-Panel 权威字段，不覆盖中央录入的名称、月费、币种和备注；
- 已实现累计流量回退检测与 `traffic_reset` 事件、按相邻快照差值计算日/月流量，以及查询时的用户到期和节点离线状态刷新；
- 已实现独立后台任务，服务启动即刷新、随后按配置周期更新用户到期状态和节点在线状态；
- 已实现 Inbound 连续三次成功完整同步缺失才归档的保护机制，并写入 `inbound_missing`、`inbound_archived` 事件；归档保留中央业务关联、Client 与历史快照；
- 已实现只读 `traffic-check` 命令，按 Dashboard 同一快照算法核对指定时间段的流量和重置基线。
- 已完成用户详情与业务字段编辑：详情返回主 Inbound、Client/Email、线路机、用户路径和最近流量快照；中央维护名称、月付/年付周期价格、币种和备注；更新写入审计日志且不会回写 X-Panel/Xray；
- 用户页面已加入精简详情抽屉、设备列表、流量重置标识和业务信息编辑表单；后端集成测试、前端类型检查和生产构建均已通过。
- 已修复用户页面 Vue 根节点校验错误：将 `ModulePage` 与 `NDrawer` 包裹在单一根元素中，提交于前端仓库 `5de6a8c fix: wrap users page with a single root element`；`pnpm typecheck` 与 `pnpm build` 均再次通过。
- W7-06 已完成：节点列表加入详情与立即同步操作，新增 `/nodes/:id` 前端页面；中央后端实现节点详情聚合接口和 `POST /api/nodes/:id/sync` 排队请求，并补充 Inbound、出口 IP、同步记录、状态事件和资源指标展示。
- W8-01 已完成：线路关系支持详情读取、新建、编辑和删除；校验线路机/落地机类型、落地 Inbound 归属、日期范围和绑定保护；线路页面已接入节点选择、状态、标签、有效期、备注及操作确认。
- W8-02 已完成：出口 IP 资产支持详情读取、新建、编辑和删除；校验 IPv4/IPv6 地址族、节点/S5 来源、成本、日期范围和线路绑定保护；出口 IP 页面支持线路机、落地机和独立 S5 来源选择。
- W8-03 已完成：线路详情提供出口 IP 绑定管理；绑定接口按出口位置校验线路机直出、落地机出口或外部 S5 的来源与节点一致性，支持启用/停用、解绑及审计日志；线路和出口 IP 列表返回来源节点与配置归属用户数，前端按出口位置筛选可绑定资产。
- 节点接入向导与节点启用/停用已完成：节点管理页新增“接入节点”入口，管理员只登记节点元数据，节点 Key 可省略并由中央自动生成，中央生成一次性 Agent Token 并展示可复制配置模板；X-Panel 凭据保留在节点本机，停用节点后 Agent heartbeat/sync 和立即同步请求都会被拒绝。
- 节点生命周期和多出口接入已补齐：节点管理支持直接删除，删除事务会清理节点及其 Inbound、Client、同步记录、流量快照、成本、出口 IP、用户路径和旧线路兼容记录，业务用户本身保留；接入向导支持一次录入多个节点出口 IP，原子写入出口资产并在列表展示数量，服务商/成本/有效期可在出口 IP 页面继续维护。
- 已加入本地 `seed-demo` 模拟数据命令：可重复写入带 `demo-*` 标识的线路机、落地机、线路、出口 IP、用户、Client 和流量快照，用于无真实 Agent 时联调用户管理与汇总页面。
- W8-04 已完成：节点详情新增节点成本记录表和录入/编辑表单；后端支持按节点保存月金额、类别、有效区间和备注，财务按月份聚合；修改成本记录时锁定生效日期，日期变更通过新增记录保留历史口径。
- W8-05 已完成：财务收入按统计月份计算有效用户，不再依赖当前用户状态；按创建时间、统计月开始时到期时间和停用状态筛选，并返回有效用户数与兼容月费收入明细，支持历史月份核对。
- W8-06 已完成：财务汇总按月份有效区间聚合节点、其他和出口 IP 成本，采用半开月份边界避免下月生效记录误计入，并返回成本明细和预计毛利润。
- W8-07 已完成：线路与出口 IP 的配置归属用户数只统计当前有效、未停用用户；出口 IP 还要求线路和资产启用，避免过期用户或停用配置继续计入。
- W8-08 已完成：线路页提供线路关系 CRUD 与出口 IP 绑定管理，财务页提供月份选择、有效用户数、收入/成本/毛利润及明细，节点详情承载节点成本记录录入与历史编辑。
- W9-01 已完成：中央 API 增加 nosniff、DENY、Referrer-Policy、Permissions-Policy、CSP 和 no-store 响应头；提供 Caddy HTTPS 反向代理示例，HSTS 仅在 HTTPS/可信代理标记下启用。
- W9-02 已完成：管理员 Bearer 写请求对 Origin/Referer 执行配置来源校验；当前认证不依赖 Cookie，保留无来源头 CLI 兼容路径，并补充跨站写入回归测试。
- W9-03 已完成：管理员 refresh token 单次轮换、logout 撤销会话；Agent 重复注册会撤销旧节点 Token 并签发新 Token，所有受保护接口校验会话/节点启用状态，补充旧凭据失效回归测试。
- W9-04 已完成：审计日志对 Token、密码、Secret、私钥、凭据、订阅链接和递归嵌套敏感字段统一脱敏，避免请求体或变更快照把明文凭据写入数据库和日志。
- W9-05 已完成：新增 `cmd/db-maintenance` 及 `deploy/backup.sh`、`migrate.sh`、`restore.sh` 和 Windows PowerShell 包装脚本；备份使用 SQLite `VACUUM INTO` 生成独立一致性快照，迁移前自动留存快照，恢复先校验源文件并创建恢复前安全快照后再原子替换数据库。
- W9-06 已完成：新增两组脱敏 X-Panel v1/v2 响应夹具，覆盖 `obj`/`data` 包装、数组/list 结构、对象/字符串 settings、clientStats/clientTraffic、数字/字符串字段，以及嵌套和扁平节点资源指标；Agent 解析器和兼容性回归测试均已通过。
- W9-07 已完成：中央与 X-Panel 客户端增加网络/超时/408/425/429/5xx 有界重试和上下文可取消退避；401 只触发一次重新登录，业务错误不重放；新增节点离线保留历史、重复 `sync_id` 压力、并发节点、接口超时和流量回退回归测试，并形成 `W9-07_FAILURE_REPORT.md`。
- W9-08 已完成：新增 `OPERATIONS_RUNBOOK.md`，覆盖中央服务与 Agent 安装、节点接入/停用/删除、线路出口来源、备份恢复、故障排查、灰度验收和回滚；新增 `deploy/xpanel-central.service` 作为中央服务 systemd 模板。
- W10-01 本地测试环境检查已完成：新增 Linux/Windows staging smoke 脚本，自动验证健康检查、安全响应头及可选管理员登录；本地 `http://localhost:8090` 冒烟通过；同时补充 Nginx 反向代理模板，公网 staging 尚未部署。
- 出口资产来源模型已完成：`exit_ips` 支持线路机/落地机节点公网出口和独立 S5，线路绑定增加 `scope=relay|landing|external`，旧 `landingNodeId` 数据与接口保持兼容；前端出口 IP 页面改为来源选择，线路绑定按出口位置筛选，线路机节点详情也展示直出 IP。
- 用户路径模型整改已完成：新增 `user_paths` 作为日常主流程；线路机从用户主 Inbound 自动确定，用户详情直接选择可选落地机/落地 Inbound 和固定出口 IP（支持线路机直出、落地机出口、独立 S5），更换/解除路径保留历史并记录审计日志。旧 `routes`、`user_routes`、`route_exit_ips` 及接口保留兼容，不再作为新用户分配的前置步骤。
- W10-02 Agent 一键部署已完成：节点接入向导针对 Ubuntu/Debian amd64 生成一行在线安装命令；中央签发 15 分钟一次性安装 Token，目标机脚本通过 GitHub 下载并校验 Agent、在本机读取 X-Panel 凭据、自动安装 systemd 服务并兑换正式节点凭据；手动 YAML 部署继续保留。
- W10-02 后续体验优化已完成：节点以 `managementUrl` 作为统一管理入口，自动解析 X-Panel 地址和 API 路径；节点列表和详情提供可点击管理地址及已有节点补填入口；接入向导支持动态添加出口 IP，旧 `publicIp` 仅保留兼容读取。
- W10-09 计费口径增强已完成：用户详情支持月付/年付及周期价格；Agent 检测线路机 Inbound 到期时间延长时生成待确认续费候选，管理员确认后写入不可变收费记录；财务按确认服务周期分摊年费，旧用户继续回退到月费统计。
- 当前本地开发服务地址：前端 `http://localhost:9527`，中央后端 `http://localhost:8090`；公网测试服务器联调尚未开始。

下一步优先级：

1. 在有公网 IP 的测试环境部署中央服务和 Agent，执行 systemd 灰度、重启恢复、真实流量核对与 HTTPS 连通性测试；
2. 接入一台线路机和一台落地机，核对线路机直出、落地机出口和独立 S5 三种出口来源；
3. 完成 W10 灰度观察后，整理正式发布、回滚和管理员培训记录。

## 2. 里程碑总览

| 里程碑 | 周期 | 交付结果 | 验收标志 |
|---|---:|---|---|
| M0 需求和基线 | 第 1 周 | 需求、口径、原型、接口基线 | 业务口径冻结 |
| M1 数据和后端骨架 | 第 2 周 | 数据库迁移、认证、基础 API | 后端可启动、迁移可重复执行 |
| M2 Agent 只读同步 | 第 3-4 周 | Agent 登录 X-Panel 并上传快照 | 测试节点同步成功 |
| M3 中央查询和汇总 | 第 5 周 | 用户、节点、流量和事件 API | API 集成测试通过 |
| M4 前端 MVP | 第 6-7 周 | Soybean Admin 精简基座、总览、用户、节点页面 | 运营流程可走通 |
| M5 线路与财务 | 第 8 周 | 线路、出口 IP、成本和收入 | 毛利润计算可核对 |
| M6 稳定性和安全 | 第 9 周 | 备份、告警、审计、兼容性 | 安全和故障测试通过 |
| M7 灰度上线 | 第 10 周 | 生产部署和文档 | 灰度运行稳定 |

## 3. 角色职责

| 角色 | 主要职责 |
|---|---|
| 产品/项目负责人 | 确认业务口径、字段、页面、优先级和验收 |
| 后端开发 | 中央 API、数据库、任务、聚合和安全 |
| Agent 开发 | X-Panel 登录、数据映射、同步和节点部署 |
| 前端开发 | 页面、筛选、状态展示和交互 |
| 测试/运维 | 测试节点、兼容性、发布、备份和故障演练 |

## 3.1 多窗口并行开发规则

建议至少使用四个开发窗口：

| 窗口 | 负责范围 | 可并行阶段 | 禁止修改 |
|---|---|---|---|
| A 后端 | 数据库、同步接收、查询、聚合、认证 | 第 1-5 周 | 前端页面和 Agent 适配器 |
| B Agent | X-Panel 登录、字段映射、心跳、同步 | 第 1-5 周 | 中央数据库迁移和前端路由 |
| C 前端 | Soybean Admin 清理、页面、组件、API client | 第 1-8 周 | X-Panel 适配逻辑和数据库迁移 |
| D 测试/运维 | 脱敏样本、测试节点、CI、部署、兼容性 | 全周期 | 业务代码核心逻辑 |

协作规则：

- 第 1 周冻结 API 类型、字段字典和状态枚举；
- 后端先提交 OpenAPI 和 JSON fixture，前端可以基于 fixture 并行开发；
- 前端窗口只负责 src/views、src/components、src/router、src/store 和 API 类型，不直接改 Agent；
- Soybean Admin 的入口、布局和依赖清理由前端窗口统一负责，其他窗口不要复制模板；
- 每个窗口使用独立分支或独立工作树，合并时按“后端契约 -> Agent -> 前端联调 -> 测试”顺序；
- 所有前端窗口使用同一个锁定的 Soybean Admin commit 和依赖锁文件；
- 任何会改变字段含义的修改必须先更新设计方案和 OpenAPI，再修改实现；
- 禁止提交模板 Mock 数据、示例路由和无关页面；
- 每日结束前发布可运行提交和变更说明，避免窗口之间依赖未提交代码。

## 4. 工作分解结构

### 第 1 周：需求冻结和技术基线

| 编号 | 任务 | 负责人 | 依赖 | 产出 |
|---|---|---|---|---|
| W1-01 | 确认线路机/落地机拓扑 | 产品/后端 | 无 | 拓扑图 |
| W1-02 | 确认“一个 Inbound=一个用户”口径 | 产品 | 无 | 业务规则清单 |
| W1-03 | 确认流量读取 X-Panel 累计字段 | 后端 | 能力总结 | 流量口径文档 |
| W1-04 | 收集 2-3 台真实节点版本和 API 响应 | 后端/运维 | 节点访问 | JSON 脱敏样本 |
| W1-05 | 确定页面范围和字段 | 产品/前端 | W1-01 | 页面线框图 |
| W1-06 | 确定技术栈、仓库结构和分支规则 | 技术负责人 | 无 | 项目 README |
| W1-07 | 编写数据库 ER 图 | 后端 | W1-02 | ER 图和字段字典 |
| W1-08 | 审计 Soybean Admin 目录、依赖和许可证 | 前端 | W1-06 | 基座裁剪清单 |
| W1-09 | 列出保留、改造、删除的模板模块 | 前端/产品 | W1-05,W1-08 | UI 范围清单 |
| W1-10 | 固定 Soybean Admin Git commit、Node 版本和锁文件策略 | 前端/技术负责人 | W1-08 | 前端基线记录 |

本周验收：

- 业务用户确定为 Inbound，而不是 Client；
- Agent 不清零 Xray 流量；
- 不承诺自动推断实际出口 IP；
- 至少拿到一台线路机和一台落地机的脱敏接口样本；
- 设计方案中的字段和术语冻结。
- Soybean Admin 保留和删除范围冻结；
- 不保留模板 Mock 数据、示例路由和 Showcase 页面；
- 已记录上游 Git commit、Node 包管理器、Node 版本和依赖锁文件；
- 已确认保留上游许可证和版权说明。

### 第 2 周：中央后端和数据库骨架

| 编号 | 任务 | 负责人 | 依赖 | 产出 |
|---|---|---|---|---|
| W2-01 | 初始化 Go 后端工程 | 后端 | W1-06 | 可运行服务 |
| W2-02 | 创建数据库迁移和索引 | 后端 | W1-07 | migration 文件 |
| W2-03 | 实现管理员登录、Session、TOTP 接口 | 后端 | W2-01 | Auth API |
| W2-04 | 实现 Token 哈希和节点凭据表 | 后端 | W2-02 | Credential service |
| W2-05 | 实现节点、用户、Inbound 基础 CRUD | 后端 | W2-02 | 管理 API |
| W2-06 | 加入统一错误码、请求 ID、结构化日志 | 后端 | W2-01 | 日志规范 |
| W2-07 | 编写 API OpenAPI 草稿 | 后端 | W2-05 | openapi.yaml |
| W2-08 | 前端复制/初始化 Soybean Admin 基座 | 前端 | W1-08 | frontend/base |
| W2-09 | 清理示例路由、Mock、Showcase 和无关菜单 | 前端 | W2-08,W1-09 | 精简前端壳 |
| W2-10 | 建立中央 API 类型和假数据 fixture | 前端/后端 | W2-07 | frontend API types |

本周验收：

- 新环境可以一键启动；
- 数据库迁移重复执行不报错；
- 管理员可以登录；
- Token 不以明文写入数据库或日志；
- API 返回格式统一；
- 前端基座可以启动，且没有模板示例页面和默认 Mock 请求。

### 第 3 周：Agent 基础和 X-Panel 登录适配

| 编号 | 任务 | 负责人 | 依赖 | 产出 |
|---|---|---|---|---|
| W3-01 | 初始化 Agent Go 工程 | Agent 开发 | W1-06 | 静态二进制 |
| W3-02 | 实现 YAML 配置和环境变量覆盖 | Agent 开发 | W3-01 | 配置加载器 |
| W3-03 | 实现 HTTPS 客户端、Token、时间戳和请求 ID | Agent 开发 | W2-04 | Central client |
| W3-04 | 实现 X-Panel Session 登录 | Agent 开发 | W1-04 | XPanel client |
| W3-05 | 实现 401 重新登录和退避 | Agent 开发 | W3-04 | 会话管理器 |
| W3-06 | 建立不同 Base Path 的测试夹具 | 测试/后端 | W1-04 | Fixture 数据 |
| W3-07 | 编写 systemd 安装和卸载脚本 | 运维 | W3-01 | service 文件 |

本周验收：

- Agent 在测试节点上可以登录 X-Panel；
- 登录失败时不会死循环；
- 密码、Cookie 和 Token 不出现在日志中；
- Agent 服务重启后可以自动恢复；
- 连接超时有明确错误码。

### 第 4 周：Inbound、Client、状态和流量同步

| 编号 | 任务 | 负责人 | 依赖 | 产出 |
|---|---|---|---|---|
| W4-01 | 解析 /inbounds/list 响应 | Agent 开发 | W3-04 | Inbound mapper |
| W4-02 | 解析 Client/Email 和设备字段 | Agent 开发 | W4-01 | Client mapper |
| W4-03 | 解析 /server/status | Agent 开发 | W3-04 | Status mapper |
| W4-04 | 读取 Inbound.up/down/all_time | Agent 开发 | W4-01 | Traffic mapper |
| W4-05 | 计算标准化配置 Hash | Agent 开发 | W4-01 | Hash service |
| W4-06 | 实现心跳和完整同步 payload | Agent 开发 | W3-03,W4-01 | Sync payload |
| W4-07 | 实现中央幂等接收和同步事务 | 后端 | W2-05,W4-06 | Sync API |
| W4-08 | 编写节点离线和重复同步测试 | 测试 | W4-07 | 集成测试 |

本周验收：

- 一台线路机和一台落地机都能同步；
- 一个 Inbound 下多个 Client 数量正确；
- 重复同步不产生重复资源；
- 中央不调用 Xray reset API；
- Server Status 版本和健康状态能展示。

### 第 5 周：汇总、到期和事件服务

| 编号 | 任务 | 负责人 | 依赖 | 产出 |
|---|---|---|---|---|
| W5-01 | 实现流量快照落库 | 后端 | W4-07 | Snapshot repository |
| W5-02 | 实现累计值回退和重置事件 | 后端 | W5-01 | Reset detector |
| W5-03 | 实现日/月流量聚合 | 后端 | W5-01 | Traffic service |
| W5-04 | 实现用户到期状态任务 | 后端 | W2-05 | Expiry job |
| W5-05 | 实现节点在线/离线判定 | 后端 | W4-07 | Health job |
| W5-06 | 实现 Inbound 缺失延迟归档 | 后端 | W4-07 | Missing policy |
| W5-07 | 实现 dashboard/users/nodes API | 后端 | W5-01,W5-04 | Query API |
| W5-08 | 编写聚合数据核对脚本 | 测试 | W5-03 | Check script |

本周验收：

- 用户列表能按到期时间筛选；
- 流量回退不产生负数；
- 节点离线保留最后快照；
- 缺失一次 Inbound 不会被删除；
- 总览 API 返回聚合值和数据时间；
- 事件可按节点和类型查询。

### 第 6 周：Soybean Admin 精简基座和总览页面

| 编号 | 任务 | 负责人 | 依赖 | 产出 |
|---|---|---|---|---|
| W6-01 | 固化精简后的 Soybean Admin 前端基座 | 前端 | W2-08,W2-09 | 前端工程 |
| W6-02 | 建立路由、布局、登录和权限守卫 | 前端 | W2-03 | 管理框架 |
| W6-03 | 接入中央 API client、类型和错误提示 | 前端 | W2-07,W2-10 | API SDK |
| W6-04 | 实现总览统计卡片 | 前端 | W5-07 | Dashboard |
| W6-05 | 实现异常事件列表 | 前端 | W5-07 | Event panel |
| W6-06 | 实现节点健康概览 | 前端 | W5-07 | Health table |
| W6-07 | 适配桌面和窄屏布局 | 前端 | W6-04 | Responsive layout |
| W6-08 | 将 Soybean 主题、菜单和标题替换为本项目内容 | 前端 | W6-01,W6-02 | Project shell |
| W6-09 | 删除残留 Mock 请求、示例资源和死链 | 前端/测试 | W6-03,W6-08 | Clean build |

本周验收：

- 登录后可以看到总览；
- 所有卡片带数据时间；
- 节点异常和同步失败有明确状态；
- 页面不展示未经确认的“实际出口用户数”；
- 空数据、加载中和错误状态完整；
- 构建产物中没有模板示例路由、Mock 数据和无关菜单。

### 第 7 周：用户和节点页面

| 编号 | 任务 | 负责人 | 依赖 | 产出 |
|---|---|---|---|---|
| W7-01 | 实现用户分页列表和筛选 | 前端 | W5-07 | Users page |
| W7-02 | 实现用户详情抽屉 | 前端 | W7-01 | User detail ✅ |
| W7-03 | 展示 Client/Email 设备列表 | 前端 | W7-02 | Device table ✅ |
| W7-04 | 展示流量趋势和重置事件 | 前端 | W5-03 | Traffic snapshots ✅ |
| W7-05 | 实现用户名称、月费、备注编辑 | 前端/后端 | W2-05 | Business fields ✅ |
| W7-06 | 实现节点详情和立即同步按钮 | 前端/后端 | W5-07 | Node detail ✅ |
| W7-07 | 加入数据过期和同步状态提示 | 前端/后端 | W7-01 | UX states ✅ |
| W7-08 | 建立项目专用状态、流量和设备组件 | 前端 | W6-01 | Shared components ✅ |
| W7-09 | 做用户列表按 Inbound 聚合的回归用例 | 前端/测试 | W7-01,W7-03 | UI regression ✅ |

本周验收：

- 一个用户显示一个 Inbound；
- Client 数显示设备数；
- 相同 Email 跨节点不冲突；
- 业务字段修改不会被下一次节点同步覆盖；
- 用户详情可以看到线路和流量；
- 设备、到期、数据延迟和同步状态组件可复用；
- 用户表不会因多个 Client 产生重复业务用户。

当前进度：第 7 周 W7-01 至 W7-09 已完成。W7-07 新增数据新鲜度、同步运行和用户到期状态组件，分页与总览接口统一返回最新成功同步时间；W7-08 新增统一流量值、流量快照、设备表和启用状态组件，并接入用户详情、节点详情和 Dashboard；W7-09 新增用户列表按 Inbound 聚合的后端集成回归用例，覆盖多 Client 不重复、相同 Email 跨节点不合并和 node_id 筛选；最近一次修复已记录在前端仓库提交 `5de6a8c`。

### 第 8 周：线路、出口 IP 和财务页面

| 编号 | 任务 | 负责人 | 依赖 | 产出 |
|---|---|---|---|---|
| W8-01 | 实现线路关系 CRUD | 后端/前端 | W2-05 | Routes API/UI ✅ |
| W8-02 | 实现出口 IP 资产 CRUD | 后端/前端 | W2-05 | Exit IP API/UI ✅ |
| W8-03 | 实现线路-出口 IP 关系 | 后端/前端 | W8-01,W8-02 | Route binding ✅ |
| W8-04 | 实现节点成本录入 | 后端/前端 | W2-05 | Node cost UI ✅ |
| W8-05 | 实现用户月费和有效用户收入 | 后端 | W2-05 | Revenue service ✅ |
| W8-06 | 实现成本汇总和毛利润 | 后端 | W8-02,W8-04,W8-05 | Finance summary ✅ |
| W8-07 | 实现线路用户数和出口 IP 配置归属数 | 后端 | W8-01,W8-03 | Allocation query ✅ |
| W8-08 | 完成线路与成本页面 | 前端 | W8-06,W8-07 | Routes/cost page ✅ |
| W8-09 | 扩展出口资产来源与线路出口位置 | 后端/前端 | W8-02,W8-03 | Relay direct / landing / external S5 ✅ |
| W8-10 | 完成用户实际线路分配闭环 | 后端/前端 | W8-01,W8-03,W8-07 | User route assignment ✅ |
| W8-11 | 按业务流程简化用户路径管理 | 后端/前端 | W8-09,W8-10 | Direct user path assignment ✅ |
| W8-12 | 分离业务用户与落地基础设施资源 | 后端/前端 | W8-11 | Path assets and landing inbound boundary ✅ |

当前进度：W8-10 用户实际线路分配闭环、W8-11 用户路径模型整改和 W8-12 业务用户/落地基础设施边界整改已完成；W9-07 故障与重试压测、W9-08 操作手册和故障手册均已完成；W10-09 计费口径增强已完成，现进入 W10 公网测试环境、真实续费核对和灰度上线。

用户详情流量趋势增强已完成：新增按 1 小时、6 小时、1 天、7 天切换的累计增长曲线和速率悬浮提示，
后端按线路机业务 Inbound 计算累计快照增量并聚合，显示累计总量、上传/下载拆分、数据覆盖率以及重置/采样缺口提示；
本阶段不修改总览页面。

W8-03 完成内容：线路详情提供出口 IP 配置入口；绑定接口按 `scope` 校验线路机直出、落地机出口或外部 S5 的来源与节点一致性，支持启用/停用、编辑、解绑及审计日志；列表返回来源节点与配置归属用户数，前端按出口位置筛选可绑定资产。

W8-04 完成内容：节点详情提供节点成本记录表；成本记录支持类别、CNY 月金额、生效日期、失效日期和备注；生效日期修改会被拒绝并提示新增版本，避免覆盖历史成本口径；节点列表和财务汇总继续按有效区间读取成本。

W8-05 完成内容：财务收入按所选月份的时间范围计算有效用户；用户必须在该月结束前已创建、统计月开始时未到期且未停用，币种为 CNY。这样当前已过期的用户仍可正确计入历史月份；接口额外返回 `effectiveUserCount`，并在明细中列出用户收入。

W8-06/W8-07 完成内容：成本按 `[月份开始, 下月开始)` 与成本记录有效区间求交集聚合，毛利润为收入减成本；旧线路统计继续兼容，出口 IP 配置归属数已切换为当前 `user_paths` 的有效用户路径统计。

W8-08 完成内容：线路页面可维护线路关系及出口 IP 绑定，财务页面可按月查询有效用户收入、成本和毛利润，节点详情成本表与财务汇总口径一致；页面构建和类型检查通过。

W8-11 完成内容：新增 `008_user_paths.sql` 迁移和用户路径 API；保存路径时自动从主 Inbound 确认线路机，校验落地机/Inbound、出口 IP 来源和启用状态，路径更换与解除保留历史。用户管理页面改为直接选择落地机、落地 Inbound 和固定出口 IP，线路关系页面隐藏为历史兼容入口；新增线路机直出、落地机出口、独立 S5、跨节点、停用资产和历史保留回归测试。

W8-12 完成内容：新增按用户聚合的路径资源接口；线路机主 Inbound 只作为业务用户来源，落地节点及其 Inbound 作为基础设施资源返回，落地 Inbound 明确标记为 `infrastructure`。用户管理页面改为显式选择线路机直出、经落地机或独立 S5 三种互斥模式；经落地机必须选择启用的落地节点、落地 Inbound 和该节点出口 IP，独立 S5 不得与落地节点混用。无首次同步时显示 `pending` 状态，不再静默显示空列表；旧线路关系接口继续兼容但不参与新路径分配。

W9-01 完成内容：`backend/internal/httpapi` 为所有 API 和健康检查统一设置安全响应头；`deploy/Caddyfile` 提供 TLS 终止、反向代理、静态前端和安全 Header 配置模板；本地 HTTP 不下发 HSTS，避免开发环境被浏览器锁定。

W9-02 完成内容：管理员写接口在 Bearer 认证成功后校验浏览器 Origin/Referer 是否属于配置的 CORS 来源；无来源头的非浏览器客户端保持兼容。由于当前前端使用 Authorization Bearer 而非 Cookie，不引入无必要的 Cookie 状态迁移。

W9-03 完成内容：refresh token 使用后立即撤销旧会话并签发新访问/刷新 Token；管理员 logout 撤销当前会话；Agent 重新注册同一节点时撤销旧节点凭据。权限检查继续要求有效 Bearer、未过期会话和启用的管理员/节点。

W9-04 完成内容：审计日志对 Token、密码、Secret、私钥、凭据、订阅链接和递归嵌套敏感字段统一脱敏，避免请求体或变更快照把明文凭据写入数据库和日志。

W9-05 完成内容：新增 `cmd/db-maintenance` 及 `deploy/backup.sh`、`migrate.sh`、`restore.sh` 和 Windows PowerShell 包装脚本。备份使用 SQLite `VACUUM INTO` 生成独立一致性快照，执行完整性/外键校验并默认保留 14 个版本；迁移前自动留存快照；恢复要求显式确认，先校验源文件、创建恢复前安全快照，再原子替换数据库并校验结果。新增迁移幂等、备份/恢复和保留策略测试。

W9-06 完成内容：Agent collector 增加 X-Panel 常见 v1/v2 字段兼容，包括 `clientStats` 流量统计、字符串/数字混合字段、`data.list` 响应包装和 `mem`/`disk` 嵌套资源指标；新增脱敏 JSON 夹具和端到端解析断言，不访问 reset 统计接口。

本周验收：

- 可以录入线路机、落地机、SS Inbound 和出口 IP；
- 可以录入月成本和用户月费；
- 收入、成本、毛利润可用手工计算结果复核；
- 页面明确使用“配置归属用户数”；
- 生效日期变更不会覆盖历史成本记录。

### 第 9 周：安全、兼容性和运维完善

| 编号 | 任务 | 负责人 | 依赖 | 产出 |
|---|---|---|---|---|
| W9-01 | HTTPS 反向代理和安全 Header | 运维 | W6-02 | Production proxy config ✅ |
| W9-02 | Cookie Secure/SameSite/CSRF 检查 | 后端 | W2-03 | Security tests ✅ |
| W9-03 | Token 轮换、撤销和权限检查 | 后端 | W2-04 | Credential controls ✅ |
| W9-04 | 审计日志和敏感字段脱敏 | 后端 | W2-06 | Audit service ✅ |
| W9-05 | 数据库备份、恢复和升级迁移 | 运维/后端 | W2-02 | Backup scripts ✅ |
| W9-06 | 多 X-Panel 版本兼容测试 | 测试 | W3-06,W4-07 | Compatibility report ✅ |
| W9-07 | 节点离线、接口超时、重试压测 | 测试 | W4-08 | Failure report ✅ |
| W9-08 | 完成操作手册和故障手册 | 技术负责人 | 全部 | Runbook ✅ |

本周验收：

- 未授权请求被拒绝；
- Token 可轮换；
- 数据库可恢复；
- 常见 X-Panel 版本均能完成只读同步；
- 故障不会造成数据删除或负流量；
- 日志中没有敏感凭据。

### 第 10 周：灰度上线和交付

| 编号 | 任务 | 负责人 | 依赖 | 产出 |
|---|---|---|---|---|
| W10-01 | 部署中央测试环境 | 运维 | W9-01 | Staging（本地 smoke ✅，公网待部署） |
| W10-02 | 接入一台线路机和一台落地机 | 运维 | W10-01 | Pilot nodes |
| W10-03 | 与 X-Panel 页面逐项核对数据 | 产品/测试 | W10-02 | Data checklist |
| W10-04 | 灰度接入 20%-30% 节点 | 运维 | W10-03 | Pilot release |
| W10-05 | 观察 3-5 天同步、离线和流量重置 | 测试 | W10-04 | Gray report |
| W10-06 | 修复阻断问题并发布正式版本 | 全员 | W10-05 | v1.0 release |
| W10-07 | 完成管理员培训和交接 | 产品/技术负责人 | W10-06 | Training notes |
| W10-08 | 建立回滚和备份确认 | 运维 | W10-06 | Rollback record |
| W10-09 | 月付/年付与 Agent 续费候选、财务历史记录 | 后端/前端 | W8-05,W10-02 | Billing records ✅ |

本周验收：

- 灰度节点连续运行期间无数据覆盖；
- 无负流量；
- 无异常 Xray 统计清零；
- 管理员可以独立完成节点接入、用户筛选、到期查看和成本录入；
- 具备明确回滚步骤。

## 5. 依赖关系

~~~text
需求冻结
   |
   +--> 数据库/后端骨架
   |          |
   |          +--> 中央同步接收
   |
   +--> Agent X-Panel 适配 --> 同步协议联调
                              |
                              +--> 流量/到期/事件服务
                                             |
                                             +--> 前端总览/用户/节点
                                             |
                                             +--> 线路/出口 IP/财务
                                                            |
                                                            +--> 安全/灰度上线
~~~

不可跨越的依赖：

- 没有真实 X-Panel 脱敏样本，不开始稳定 Agent 适配；
- 没有固定用户和流量口径，不开始报表开发；
- 没有 Token 和 HTTPS 方案，不接入生产节点；
- 没有备份恢复演练，不进行全量节点接入。

### 5.1 可并行工作流

在第 2-5 周可以同时推进以下工作流：

| 工作流 | 第 2-3 周 | 第 4-5 周 | 联调点 |
|---|---|---|---|
| 后端窗口 | 迁移、认证、节点 API | 同步接收、快照、聚合 | W5-07 API |
| Agent 窗口 | 配置、登录、Session | 映射、心跳、完整同步 | W4-07 Sync API |
| 前端窗口 | Soybean 基座裁剪、路由壳 | API 类型、页面骨架和 fixture 页面 | W6-03 API client |
| 测试运维窗口 | 节点样本、CI、安装脚本 | 故障注入、兼容性和数据核对 | W10 灰度 |

前端不必等待后端页面全部完成：拿到 OpenAPI 和 fixture 后即可开发页面；Agent 不必等待前端完成；后端和 Agent 只在同步 payload 与错误码处联调。这样可以支持多个窗口同时开发，同时保持清晰的合并边界。

## 6. 每周项目检查点

| 周 | 检查问题 |
|---:|---|
| 1 | 业务用户是否确定为 Inbound，而不是 Client？ |
| 2 | 数据库迁移和认证是否可重复部署？ |
| 3 | Agent 是否能处理 Session 过期和 Base Path？ |
| 4 | 是否只读取 X-Panel 累计流量，没有 reset？ |
| 5 | 节点离线和流量重置是否保留历史？ |
| 6 | 总览指标是否有数据时间和异常状态？ |
| 7 | 用户详情是否正确显示多个设备？ |
| 8 | 毛利润是否能用人工账目复核？ |
| 9 | Token、CSRF、备份和兼容性是否通过？ |
| 10 | 灰度节点是否稳定，是否具备回滚方案？ |

## 7. 测试计划

### 7.1 测试数据集

至少准备：

- 1 台线路机，2 个业务 Inbound；
- 每个业务 Inbound 2-3 个 Client；
- 1 台落地机，1 个 SS 基础设施 Inbound；
- 一台不同 Base Path 的 X-Panel；
- 一个有流量增长的 Inbound；
- 一个手动重置过流量的 Inbound；
- 一个被删除后重新创建的 Inbound；
- 不同节点使用相同 Email 的样本。

### 7.2 必测故障

- X-Panel 登录返回 401；
- X-Panel 暂时不可达；
- 中央服务超时；
- 重复发送同一 sync_id；
- 同步过程中部分 Inbound 解析失败；
- 节点连续离线；
- Inbound 在一次同步中消失；
- all_time 下降；
- 数据库恢复到旧快照；
- Agent 升级后回滚。

### 7.3 前端基座回归

- Soybean Admin 原始示例路由全部移除；
- Mock 数据、随机图表和示例请求全部移除；
- 菜单只保留总览、用户、节点、线路与出口 IP、成本财务、同步事件；
- 登录、Session 刷新、退出和路由守卫正常；
- 用户表按 Inbound 聚合，Client 只显示为设备；
- 真实 API 的分页、筛选、空状态、加载状态和错误状态正常；
- 主题切换、表格横向滚动和窄屏布局不遮挡核心数据；
- 构建产物没有无关页面、无效菜单或死链。

### 7.4 性能基线

MVP 建议验证以下数量级：

| 指标 | 目标 |
|---|---:|
| 节点数 | 100 台 |
| 每节点 Inbound | 500 个 |
| 单次同步响应 | 30 秒内 |
| 中央查询 P95 | 500 ms 内 |
| 心跳处理 P95 | 200 ms 内 |
| 同步失败重试 | 不阻塞其他节点 |
| 页面列表 | 单页 50 条，最大 200 条 |

如果实际节点规模超过上述基线，再评估 PostgreSQL、队列和分布式任务调度。

## 8. 交付物清单

### 8.1 代码

- 中央后端服务；
- Agent 服务；
- 基于 Soybean Admin 精简后的 Vue 前端；
- 本项目专属页面和组件，不包含模板示例业务；
- 数据库迁移；
- API 类型和 OpenAPI 文件；
- systemd、Nginx/Caddy 和备份脚本；
- 测试夹具和集成测试；
- 发布和回滚脚本。

### 8.2 文档

- 部署手册；
- 节点接入手册；
- 节点停用和 Token 轮换手册；
- 故障排查手册；
- 数据口径说明；
- 数据库备份恢复手册；
- 版本发布和回滚记录模板；
- 管理员操作手册。

## 9. 发布门禁

正式发布前必须全部满足：

- [ ] 所有数据库迁移在空库和已有库上通过；
- [ ] Agent 不调用 QueryStats(reset=true)；
- [ ] X-Panel 页面数据与中央快照核对通过；
- [ ] Soybean Admin 示例路由、Mock 数据和 Showcase 页面已清理；
- [ ] 前端菜单只包含本项目业务模块；
- [ ] 用户页面按 Inbound 聚合，多个 Client 不重复计数；
- [ ] 多 Client 不重复计算用户数；
- [ ] 跨节点相同 Email 不覆盖；
- [ ] 流量重置不出现负数；
- [ ] 节点离线不删除历史；
- [ ] Token 不明文存储；
- [ ] HTTPS、CSRF、Cookie 安全属性通过检查；
- [ ] 备份恢复演练成功；
- [ ] 灰度节点至少连续运行 3 天；
- [ ] 回滚步骤已验证；
- [ ] 管理员操作手册完成。

## 10. 后续版本排期建议

### v1.1：运营增强，预计 2-3 周

- CSV/Excel 导出；
- 到期提醒通知；
- 更多流量趋势和节点排行；
- 配置漂移差异查看；
- 多成本分摊规则；
- 更细的事件筛选。

### v2.0：中央写操作，预计 4-6 周

- 中央创建用户；
- 添加和删除设备 Client；
- 续期、禁用和批量操作；
- Agent 任务队列和重试；
- 节点执行结果回传；
- 完整审计和审批确认。

### v3.0：财务和精确归因，单独评估

- 支付记录和账期；
- 成本按用户/线路/流量分摊；
- 用户实际出口归因；
- 多管理员角色和多租户。

## 11. 进度偏差处理

如果进度延期，按以下优先级裁剪：

1. 保留节点同步、用户列表、到期时间和流量；
2. 暂缓历史趋势和复杂图表；
3. 暂缓出口 IP 分摊，只保留资产录入；
4. 暂缓中央写操作；
5. 不裁剪安全、备份、幂等和流量重置检测。

不建议通过关闭安全校验、直接暴露 X-Panel 或让中央清零 Xray Counter 来赶进度。

## 12. 项目完成定义

项目 v1.0 完成的标准是：

~~~text
中央面板能够稳定接入线路机和落地机，
按 Inbound 展示业务用户，
正确显示多个 Client 设备，
读取 X-Panel 累计流量，
显示到期和节点状态，
维护线路、出口 IP、成本和月费，
计算收入、成本和预计毛利润，
并在节点离线、流量重置或同步异常时保留可解释的数据状态。
~~~
